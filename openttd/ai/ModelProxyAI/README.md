# ModelProxyAI Foundation Package

`ModelProxyAI` establishes the benchmark company boundary and is otherwise inert. It contains no construction, vehicle, finance, or order API calls. Future heartbeat behavior must be added through a versioned contract.
