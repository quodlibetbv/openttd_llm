# Arena Overlay Foundation

This TypeScript project is the Phase 00 projection boundary for the future OBS Browser Source. It contains a deterministic state reducer and safe text rendering through `textContent`; it does not connect to a run, render provider payloads, or control OBS yet.

Run `npm ci --prefix src/Arena.Overlay`, then `npm test --prefix src/Arena.Overlay` to compile and test the scaffold.
